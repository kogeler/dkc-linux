# Fixtures

Synthetic inputs for the `fast` tier: version-ordering cases, signature failure
and expiry cases, multi-series retention sets including epochs, release
candidates, Debian revisions and repeated DKC revisions, CAS and failure
injection sequences, and secret-leak canaries.

Canary values are synthetic. Real secret-bearing diagnostic output is never
preserved.
